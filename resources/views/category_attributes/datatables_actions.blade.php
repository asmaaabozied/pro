{!! Form::open(['route' => ['categoryAttributes.destroy', $id], 'method' => 'delete']) !!}
<div class='btn-group'>
    @can('categoryAttributes.list')
        <a href="{{ route('categoryAttributes.show', $id) }}" class='btn btn-primary btn-xs'>
            <i class="glyphicon glyphicon-eye-open"></i>
        </a>
    @endcan
    @can('categoryAttributes.edit')
        <a href="{{ route('categoryAttributes.edit', $id) }}" class='btn btn-success btn-xs'>
            <i class="glyphicon glyphicon-edit"></i>
        </a>
    @endcan
    @can('categoryAttributes.delete')
        {!! Form::button('<i class="glyphicon glyphicon-trash"></i>', [
            'type' => 'submit',
            'class' => 'btn btn-danger btn-xs',
            'onclick' => "return confirm('Are you sure?')"
        ]) !!}
    @endcan
</div>
{!! Form::close() !!}
