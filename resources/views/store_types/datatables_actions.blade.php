{!! Form::open(['route' => ['storeTypes.destroy', $id], 'method' => 'delete']) !!}
<div class='btn-group'>
    @can('storeTypes.list')
        <a href="{{ route('storeTypes.show', $id) }}" class='btn btn-primary btn-xs'>
            <i class="glyphicon glyphicon-eye-open"></i>
        </a>
    @endcan
    @can('storeTypes.edit')
        <a href="{{ route('storeTypes.edit', $id) }}" class='btn btn-success btn-xs'>
            <i class="glyphicon glyphicon-edit"></i>
        </a>
    @endcan
    @can('storeTypes.delete')
        {!! Form::button('<i class="glyphicon glyphicon-trash"></i>', [
            'type' => 'submit',
            'class' => 'btn btn-danger btn-xs',
            'onclick' => "return confirm('Are you sure?')"
        ]) !!}
    @endcan
</div>
{!! Form::close() !!}
